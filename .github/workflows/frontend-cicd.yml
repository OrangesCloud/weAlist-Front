name: Frontend CI/CD to S3/CloudFront

on:
  push:
    branches:
      - main
  workflow_dispatch:

# env section removed - values loaded from Parameter Store in steps

jobs:
  deploy:
    runs-on: ubuntu-latest
    steps:
      - name: ⬇️ Checkout Repository
        uses: actions/checkout@v4

      # ============================================
      # 1. AWS Credentials 설정 (백엔드 공유 키 사용)
      # ============================================
      - name: 🔐 Configure AWS Credentials (for all tasks)
        uses: aws-actions/configure-aws-credentials@v4
        with:
          aws-access-key-id: ${{ secrets.FRONTEND_DEPLOY_AWS_ACCESS_KEY_ID }}
          aws-secret-access-key: ${{ secrets.FRONTEND_DEPLOY_AWS_SECRET_ACCESS_KEY }}
          aws-region: ap-northeast-2

      # ============================================
      # 2. Setup (pnpm/Node.js)
      # ============================================
      - name: ⚙️ Setup pnpm
        uses: pnpm/action-setup@v2
        with:
          version: 8.15.0

      - name: ⚙️ Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '20'
          cache: 'pnpm'
          cache-dependency-path: 'pnpm-lock.yaml' # 📂 경로 수정됨

      # ============================================
      # 3. SSM Parameter Store에서 ALB 도메인 조회
      # ============================================
      - name: 🌍 Fetch ALB Domain from SSM
        run: |
          # ALB 도메인 조회
          ALB_DOMAIN=$(aws ssm get-parameter --name "/wealist/dev/alb/domain" --query "Parameter.Value" --output text)

          # GITHUB_ENV에 주입 (Vite 빌드에 사용)
          echo "VITE_API_BASE_URL=https://$ALB_DOMAIN" >> $GITHUB_ENV

          echo "✅ Fetched API Base URL: https://$ALB_DOMAIN"

      - name: 📦 Install Dependencies
        run: pnpm install --frozen-lockfile # 📂 cd frontend 제거

      # ============================================
      # 4. Build with Vite
      # ============================================
      - name: 🔨 Build with Vite
        run: pnpm run build # 📂 cd frontend 제거

      # ============================================
      # 5. Deploy & Invalidate Cache
      # (나머지 S3/CloudFront 배포 로직은 최적화되어 있으므로 그대로 유지)
      # ============================================
      - name: ⬆️ Deploy to S3 (Optimized Cache Control Applied)
        run: |
          aws s3 sync ./dist s3://${{ secrets.FRONTEND_S3_BUCKET_NAME }}/ \
            --delete \
            --cache-control "public, max-age=31536000, immutable" \
            --exclude "index.html"
            
          aws s3 cp ./dist/index.html s3://${{ secrets.FRONTEND_S3_BUCKET_NAME }}/index.html \
            --cache-control "no-cache, no-store, must-revalidate" \
            --content-type "text/html"

      - name: 🌐 Invalidate CloudFront Cache (Only for index.html)
        run: |
          aws cloudfront create-invalidation \
            --distribution-id ${{ secrets.FRONTEND_CLOUDFRONT_ID }} \
            --paths "/index.html"

      - name: ✅ Deployment Complete
        run: echo "✅ Frontend deployed successfully!"
